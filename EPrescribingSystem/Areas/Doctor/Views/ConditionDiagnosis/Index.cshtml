@model IEnumerable<EPrescribingSystem.Models.ConditionDiagnosis>

@{
    ViewData["Title"] = "Condition Diagnosis Records";
    Layout = "_Layout";
}
<head>
    <link rel="stylesheet" href="~/parent/pcalendar/assets/bootstrap/css/bootstrap.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.bundle.min.js"></script>
</head>

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.bundle.min.js"></script>

@*Write Code*@
<div class="text-center">
    <h1>Condition Diagnosis Records</h1>
</div>

<br />
<br />
<div class="row">
    <div class="align-items-center mb-4">
        <div class="table-responsive-sm">
            <table id="example" class="display nowrap" style="width:100%">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ICD_10_CODE)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Diagnosis)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DiagnosisDate)
            </th>
            <th>
               Patient
            </th>
              <th>
                @Html.DisplayNameFor(model => model.Medication)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Allergy)
            </th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ICD_10_CODE)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Diagnosis)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DiagnosisDate.Date)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.ApplicationUser.FirstName)   @Html.DisplayFor(modelItem => item.ApplicationUser.LastName)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Medication.Name)
                </td>
            <td>
                @Html.DisplayFor(modelItem => item.Allergy)
            </td>
            <td>
                <a asp-controller="ConditionDiagnosis" asp-action="Edit" asp-route-id="@item.ConditionID" class="btn btn-outline-primary"><i class="fas fa-pencil-alt"></i> Edit</a> |
                <a asp-controller="ConditionDiagnosis" asp-action="Details" asp-route-id="@item.ConditionID" class="btn btn-outline-info"><i class="fas fa-eye"></i>Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ConditionID" class="btn btn-danger text-white"><i class="fas fa-trash-alt"></i>Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
        </div>
    </div>
</div>

<div style="right: 25px;" class="text-white">
    <a asp-controller="ConditionDiagnosis" asp-action="Create" class="btn btn-success"> <i class="bi bi-plus-circle"></i> Add New Diagnosis </a>
</div>
<script type="text/javascript">
    function openSuccessModal(strMessage) {
        var myDiv = document.getElementById("MyModalSuccessAlertBody");
        myDiv.innerHTML = strMessage;
        $('#myModalSuccess').modal('show');

        window.setTimeout(function () {
            $('#myModalSuccess').modal('hide');
        }, 3000);
    }

    $(document).ready(function () {
        var msg = "@TempData["SuccessMessage"]";

        if (msg) {
            openSuccessModal(msg);
        }

        //debugger;
    });
</script>

<script>
    $(document).ready(function () {
        $('.navbar-dark').find('[href="' + window.location.pathname + '"]').parent().addClass('active')
    });
</script>