@using EPrescribingSystem.Models
@*Define Models*@
@model IEnumerable<Pharmacy>
@{
    ViewData["Title"] = "List of Pharmacy";
    Layout = "_Layout";
}

@*Write Code*@
<div class="text-center">
    <h1>Welcome, Here are all the Pharmacy in our system.</h1>
</div>

<br />
<br />
<div class="row">
    <div class="align-items-center mb-4">

        <table id="example" class="display nowrap" style="width:100%">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.LicenseNumber)</th>
                    <th>@Html.DisplayNameFor(model => model.Name)</th>
                    <th>@Html.DisplayNameFor(model => model.Address1)</th>
                    <th>Suburb</th>
                    <th>@Html.DisplayNameFor(model => model.ContactNumber)</th>
                    <th>@Html.DisplayNameFor(model => model.EmailAddress)</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="align-middle">
                            @Html.DisplayFor(modelitem => item.LicenseNumber)
                        </td>
                        <td class="align-middle">
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td class="align-middle">
                            @Html.DisplayFor(modelItem => item.Address1)
                        </td>
                        <td class="align-middle">
                            @Html.DisplayFor(modelItem => item.SuburbModel)
                        </td>
                        <td class="align-middle">
                            @Html.DisplayFor(modelItem => item.ContactNumber)
                        </td>
                        <td class="align-middle">
                            @Html.DisplayFor(modelItem => item.EmailAddress)
                        </td>
                        <td class="align-middle">
                            <a asp-controller="Pharmacy" asp-action="Edit" asp-route-id="@item.PharmacyID" class="btn btn-outline-primary"><i class="bi bi-pencil-square"></i>Edit </a> |
                            <a asp-controller="Pharmacy" asp-action="Details" asp-route-id="@item.PharmacyID" class="btn btn-outline-info"><i class="bi bi-eye"></i>Details </a> |
                            <a asp-action="Delete" asp-route-id="@item.PharmacyID" class="btn btn-danger text-white"><i class="bi bi-trash"></i>Delete </a>
                        </td>
                    </tr>
                }
            </tbody>

        </table>

    </div>
</div>

<div style="position: fixed; right: 25px;" class="text-white">
    <a asp-controller="Pharmacy" asp-action="Create" class="btn btn-success"> <i class="bi bi-plus-circle"></i> Add New Pharmacy </a>
</div>